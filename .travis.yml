language: python

# Needed for postgres stuff
sudo: required

services:
    - postgresql

addons:
    postgresql: "9.5"

python:
  - "2.7"
  - "3.4"
  - "3.5"

before_install:
  - ./tools/travis-before-script.sh

install:
  # Install postgis in test database
  - psql postgres://postgres@localhost/travis -c "create extension postgis"

  # Piece of shit to get a correct version of node through nvm
  - rm -rf ~/.nvm && git clone https://github.com/creationix/nvm.git ~/.nvm && (cd ~/.nvm && git checkout `git describe --abbrev=0 --tags`) && source ~/.nvm/nvm.sh && nvm install $TRAVIS_NODE_VERSION

  # Get an up-to-date pip to verify hashes
  - pip install -U pip
  - "pip install -r dev-requirements.txt --no-binary :all: --no-deps --require-hashes"

  # Install compass
  - gem update --system
  - gem install sass -v 3.2.13
  - gem install compass

  # Install grunt-cli and grunt
  - npm install -g grunt-cli
  - npm install

script:
  - grunt build
  - nosetests

after_success:
  - if [[ $TRAVIS_BRANCH == 'master' ]] && [[ $TRAVIS_PULL_REQUEST == 'false' ]] && [[ $TRAVIS_PYTHON_VERSION == '2.7' ]]; then
      ./tools/travis-deploy.sh;
    fi

env:
  global:
    - TRAVIS_NODE_VERSION="4"
    - DATABASE_URL='postgresql://travis@localhost/travis'

    # GitHub access token that grants access to thusoys GitHub account
    - secure: "C25TjyugCENUdPyewhln/jMF+1I1kcKvckzybI4fs7USPzMSAf04HtPHghmHoJfq9FtKg/gZzW253B21wTrISZyRq5vEFXCcmeikVlqjGAy23CzS2vE7c1qgyRQ8x+UQlbAd82Hl+nb9mgE+85az8QRy/5vUGT5fLdCa0s4azGc="

notifications:
  email: false

branches:
  except:
    - production
